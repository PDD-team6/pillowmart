create database pillowmart;

use pillowmart;
create table login(
username varchar(50),
password varchar(50)
);
select * from login;
create table payment(
fullname varchar(50),
email varchar(50),
address varchar(50),
city varchar(50),
state varchar(50),
cardname varchar(50),
cardnumber varchar(50),
expmonth varchar(50),
expyear varchar(50)

);
